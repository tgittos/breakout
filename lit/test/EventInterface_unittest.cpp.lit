== Event Interface Unit Tests ==
The event interface is a class that the subscribers/
notifiers can compose into themselves to give themselves
eventing capability. This unit test file will test
the functionality of the interface talking to the
EventManager

== Interface registers for event from manager ==
-
TEST(EventInterface, RegisterEvent) {
  ASSERT_TRUE(false);
}
-

== Interface dispatches event to manager ==
-
TEST(EventInterface, DispatchEvent) {
  ASSERT_TRUE(false);
}
-

== Includes ==
-
#include "gtest/gtest.h"
#include "EventInterface.hpp"
-

== Wrapper ==
-
Includes.

Interface registers for event from manager.

Interface dispatches event to manager.
-

== @EventInterface_unittest.cpp ==
Wrapper.