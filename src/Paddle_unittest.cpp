#include "gtest/gtest.h"
#include "Paddle.hpp"

TEST(PaddleTest, AccelerationOfPaddle) {
  ASSERT_FALSE(true);
}

TEST(PaddleTest, DecelerationOfPaddle) {
  ASSERT_FALSE(true);
}

TEST(PaddleTest, InstantaneousDirectionChange) {
  ASSERT_FALSE(true);
}

TEST(PaddleTest, ListenForKeyboardEvents) {
  ASSERT_FALSE(true);
}
